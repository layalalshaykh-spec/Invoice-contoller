import { apService } from "@/lib/services/ap-service";
export async function POST(request:Request,{params}:{params:Promise<{id:string}>}) { const {id}=await params; const body=await request.json() as {decision?:"APPROVE"|"REJECT"|"REQUEST_INFO";exceptionId?:string;userId?:string;note?:string}; if(!body.decision||!body.userId)return Response.json({error:"decision and userId are required"},{status:400}); try{return Response.json(await apService.decide({invoiceId:id,exceptionId:body.exceptionId,decision:body.decision,userId:body.userId,note:body.note}))}catch(error){return Response.json({error:error instanceof Error?error.message:"Decision failed"},{status:400})} }
